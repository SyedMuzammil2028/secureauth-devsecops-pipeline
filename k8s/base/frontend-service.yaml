apiVersion: v1
kind: Service
metadata:
  name: secureauth-frontend
  labels:
    app.kubernetes.io/name: secureauth
    app.kubernetes.io/component: frontend
spec:
  selector:
    app.kubernetes.io/name: secureauth
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 8080
      targetPort: http

